{
  "name": "cs",
  "version": "1.8.1",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "check": "bun x tsc --noEmit && bun scripts/sync-version.ts --check",
    "version:check": "bun scripts/sync-version.ts --check",
    "build": "bun scripts/sync-version.ts && bun build cs.ts --outfile cs.bundle.js --target bun"
  },
  "dependencies": {
    "mongodb": "^6.12.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
