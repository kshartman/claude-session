{
  "name": "cs",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "check": "bun x tsc --noEmit",
    "build": "bun build cs.ts --outfile cs.bundle.js --target bun"
  },
  "dependencies": {
    "mongodb": "^6.12.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
